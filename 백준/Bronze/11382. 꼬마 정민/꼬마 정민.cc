#include <iostream>
#include <algorithm>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    long num1,num2,num3;
    cin >> num1 >> num2 >> num3;
    cout << num1 + num2 + num3;
    return 0;
}